$(document).on 'page:reloaded', ->
  $('.pdf-pages').each ->
    Galleria.run '.pdf-pages',
      responsive:true
      height:0.8
      transition: 'flash'
      swipe: true

  $('.share-btn').click ->
    $('.shares').slideToggle()
    false
